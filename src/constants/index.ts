export const CDN_URL = `${process.env.API_ORIGIN}/content/weblarek`;
export const API_URL = `${process.env.API_ORIGIN}/api/weblarek`;

export const CATTEGORIES_MAP: Record<string, string> = {
    "дополнительное": "additional",
    "софт-скил": "soft",
    "кнопка": "button",
    "хард-скил": "hard",
    "другое": "other",
}